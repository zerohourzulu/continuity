# Source-policy recovery results

First frozen candidate, document-reference0.1.13: all four new focused groups and the selected existing G1 lifecycle regression passed on 18 September 2026 at20:17PDT. Both invocations exit0, total controller-observed4.783639251seconds. New-test TAP counts5 including its parent; legacy G1 counts1. No rerun or post-freeze correction. Two source-review findings were corrected before freeze: physical path overlap and a test's structured acknowledgment assertion. Root also corrected the recovery return's missing lockPresent field before freeze.

The copied three-entry store retains exact header/entry bytes and final REVOKED selection, while the original including its lock remains unchanged. The unlocked destination supports normal acquire/current/release. Passive inspection is exercised with write-capable APIs prohibited. The actual case-insensitive path alias is refused before creation on this host (not skipped). An independently supplied later head rejects an old enabled backup; malformed tail, missing middle entry, unsafe files/links, overlap, existing output and invalid assertions refuse without salvage.

A deliberately timed real input change during copy produces UNCONFIRMED and preserves the changed original. Injected file-sync failure leaves a partial destination/lock. Injected failure after final unlink leaves no lock and still reports UNCONFIRMED, without manufacturing one. Actual CLI stdout inherited as a read-only descriptor yields exit2 even though exact copy bytes exist. A later observation does not reconstruct the missing acknowledgment.

The unchanged existing G1 verifies enrollment, status, selection, stale-head refusal, bounded chains, key-ID binding, metadata behavior, repeated release, changed lock/header and uncertain append. Only G1 ran; the prior G2–G4/Core-case campaign was not reopened. Static imported Core/fixture dependencies are external prerequisites for G1, but no Core history or agent operation was executed.

Actual use:14root slots,7CLI children with exits0/0/2/0/0/0/1; zero Core histories, fresh random keys and document effects. The new fixture uses public policy bytes only. G1 uses its existing known-public signing fixture.213regular fixture files are retained;2symlinks recorded without following.68frozen inputs and12,614external dependency files verified unchanged before/after;21syntax checks pass.

Evidence: [new run](evidence/recovery-original.json), [new full log](evidence/recovery-original.log), [G1 run](evidence/regression-g1-original.json), [G1 full log](evidence/regression-g1-original.log), [collection](evidence/collection.json), [freeze](evidence/frozen-inputs.json), [post-run identities](evidence/post-run-identities.json), [source review](review/SOURCE-REVIEW.md).

Limits: trusted cooperating local host, explicit unproven quiescence assertion and caller-selected head. No independent global freshness, rollback oracle, production custody, all-crash-schedule proof, hostile-owner containment, native installation, automatic cutover or old acknowledgment recovery. Current revocation is preserved relative to the complete selected chain; a stale caller anchor cannot rule out an omitted later state.
